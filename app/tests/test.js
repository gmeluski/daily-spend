var assert = require('chai').assert();
var spendModel = require('../models/spend');

describe('testing that the aggregate function works', function (){
   it('should have five records', function() {
       
       
   }); 
    
});
